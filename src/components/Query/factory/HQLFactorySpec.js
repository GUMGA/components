describe('PROVIDER: GumgaAlert', function() {
	let Service = {};

	beforeEach(module('gumga.query'));

	beforeEach(inject((HQLFactory) => {
    Service = HQLFactory;
  }))

  it('Should pimba', () => {
  })

});
